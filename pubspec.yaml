name: edupageapi
description: An EduPage API library using the EduPage2 backend servers.
version: 1.0.0
repository: https://github.com/DislikesSchool/EduPageAPI-dart

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  connectivity_plus: ^6.1.0
  dio: ^5.7.0
  intl: ^0.19.0
  shared_preferences: ^2.3.3
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
